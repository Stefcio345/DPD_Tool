<MudMenuItem Class="px-1 py-0 my-0">
    <MudStack>
        <MudButton Style="justify-content: left" OnClick="@(() => { MethodSelected(Method, System); })" Variant="Variant.Filled">
            <MudText>
                @Name
            </MudText>
        </MudButton>
    </MudStack>
</MudMenuItem>


@code
{
    [Parameter] public SOAP_API_METHODS Method { get; set; }
    [Parameter] public API_SYSTEM System { get; set; }
    [Parameter] public string Name { get; set; }
    [Parameter] public SoapData Data { get; set; }
    [Parameter] public SoapInterface Parent { get; set; }
    
    //private Color GetButtonColor(SOAP_API_METHODS currentMethod)
    //{
        //return currentMethod == Data.CurrentMethod ? Color.Primary : Color.Default;
    //}
    
    private void MethodSelected(SOAP_API_METHODS obj, API_SYSTEM system)
    {
        //Data.CurrentMethod = obj;
        //Data.CurrentSystem = system;
        Parent.UpdateRequest();
        StateHasChanged();
    }
}
